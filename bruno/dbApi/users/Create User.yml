info:
  name: Create User
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/users"
  body:
    type: json
    data: |-
      {
        "email": "test3@test.tes",
        "username": "12345",
        "first_name": "james",
        "last_name": "malarkey",
        "birth_date": "2002-12-17T00:00:00Z"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
